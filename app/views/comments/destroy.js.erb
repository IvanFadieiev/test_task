$('.deleteAction').bind('ajax:success', function() {
	if ($(this).closest('.comment').next().hasClass("comment_list")) {
		$(this).closest('.comment').slideUp(300);
		$(this).closest('.comment').next().slideUp(300);
    }
    else{
    	$(this).closest('.comment').slideUp(300);
    } 	
});